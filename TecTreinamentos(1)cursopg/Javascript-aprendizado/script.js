// Declaração Variáveis e Constantes

// Comentário em linha.

/*
let mensagem = true      let= Variavel que pde ser modificada.
console.log(mensagem)    const= Variavel que não pode ser feita modificação.
console.log(typeof(mensagem))     Para comentar um bloco de comando.
*/

// Funções 

function somar(numA,numB) {
    return numA + numB
}

let resultado = somar(10, 3)
console.log(resultado)

// Vetores

